Stop a bridge worker and update inventory.

Usage: /stop-bridge --user <handle>

Steps:
1. Run: `operator/scripts/stop-bridge.sh --user <handle>`
   - Kills the process and updates bridge-inventory.md automatically
